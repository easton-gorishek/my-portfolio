import React from 'react';
import styles from './footer.module.css';

export default () => (
  <footer className={styles.footer}>
    <p className={styles.copyRight}>&copy; easton gorishek 2019</p>
  </footer>
);
